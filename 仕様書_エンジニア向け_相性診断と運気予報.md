# 実装仕様書（エンジニア向け・完全版）
## 「奇跡の相性診断 1.66」＋「私だけの運気予報」

- 版：**2026-08-28 / フロント v.0824-226 準拠**
- 対象読者：この2サイトを構築・保守・バックエンド実装するエンジニア
- 位置づけ：**この文書が最新の正本**。旧 `SPEC-実装仕様書.md`／`HANDOFF.md` と食い違う場合は本書を優先（旧文書の月額課金・SBPS決済・トライアルの記述はすべて無効。現行は「完全無料＋広告モデル」）。

---

## 0. 最重要・3原則（絶対に守ること）

1. **四柱推命エンジンには手を入れない。**
   `bazi.js` / `engine.js` / `daily-engine.js` と、各HTML内の命式・スコア・グレード計算は**検証済みの確定ロジック**。文言・CSS・画像は自由に変えてよいが、**計算式・定数・シード・プール長は改変しない**（下§4〜§6の定数を変えると全画面の診断結果がズレる）。

2. **メールとカレンダーは同一エンジンで生成し、必ず一致させる。**
   毎朝メールのグレード（💮◎〇△）・こよみマークは、マイページのカレンダーと**同一の `bazi.js`／`daily-engine.js`** で算出すること。別実装・別バージョン禁止。詳細は§11の「必須契約」。

3. **タイプ名・ハッシュタグ・干支データには唯一の正本がある。**
   60タイプ名は `index.html` の `TYPE_NAME`、ハッシュタグは `KANSHI_TAGS` が正本。図鑑(`zukan.html`)・マスコット説明(`MASCOT_INFO`)はそれに追従。**片方だけ直すとズレる**（§7）。

---

## 1. 2つのプロダクト概要

| | 奇跡の相性診断 1.66 | 私だけの運気予報 |
|---|---|---|
| 本番ドメイン | **166unmei.com** | **unkiyoho.jp** |
| 役割 | 2人の生年月日で相性を診断（集客の入口・**登録不要で遊べる**） | 毎朝、四柱推命の個別鑑定を**メール配信**する本商品 |
| 収益モデル | **完全無料＋広告** | **完全無料＋広告＋登録属性の広告活用** |
| バックエンド | **不要**（完全クライアント処理） | **要実装**（登録受付・毎朝バッチ・配信・オプトアウト） |
| 中心ファイル | `index.html` | `lp.html`→`register.html`→毎朝メール→`mypage.html` |

- ユーザー動線：SNS広告 → 相性診断で遊ぶ → 運気予報LPで無料登録 → 毎朝メール受信。
- **旧モデル（月額480円サブスク／7日トライアル／SBPS決済）は全廃**。会員種別（無料/有料）の分岐は作らない＝**全員フル機能**。

---

## 2. デプロイ（両サイト共通）

- **ビルド不要の静的サイト**。フォルダ一式を静的ホスティング（Netlify / Vercel / S3 / 任意のWebサーバ）に置くだけで動く。
- 配布zipは**フラット構成**（全ファイルがルート直下）。そのままドキュメントルートへ配置。
- **外部依存は Google Fonts のみ**（オフライン時もフォールバック表示可）。
- **必ずWebサーバ経由で開く**。`file://` でのダブルクリックは不可（画像・iframe埋め込みメール見本が読めず「空」に見える）。ローカル確認例：`python3 -m http.server` → `http://localhost:8000/index.html`。
- `_headers`（Netlify形式）を同梱。キャッシュ制御等を行っている。他ホスティングでは相当の設定に読み替え。
- **タイムゾーンは全処理 JST 固定**（「今日」判定・配信時刻）。UTCで計算すると干支が1日ズレる。

### 配布zip（相性診断）
| zip | 用途 | デモボタン |
|---|---|---|
| `166unmei_本番_デモ削除.zip` | **本番公開用** | 削除済み（`_demoBypass(){return false}`） |
| `166unmei_deploy_FLAT.zip` | 動作確認・引き継ぎ用 | あり（左下の「デモ：回数制限を解除」） |

→ **公開には必ず「本番_デモ削除」版を使う**。FLAT版のデモ解除ボタンは開発確認専用で、本番に載せてはいけない。

---

## 3. ファイル構成

### 相性診断（166unmei.com）
| ファイル | 役割 |
|---|---|
| `index.html` | **本体**。相性診断UI＋スコア／タイプ名／ハッシュタグ／マスコットのデータと描画。**登録ゲートもここ**（§8） |
| `zukan.html` | 60干支の図鑑。`TYPE_NAME` に追従（§7） |
| `lp.html` | 運気予報LP（相性診断からの送客先を兼ねる） |
| `register.html` | 登録フォーム（送信先は要差し替え・§12） |
| `mypage.html` | マイページ（命式・運命カレンダー・配信設定）。四柱推命エンジン内蔵 |
| `my-tenki-demo.html` | 登録不要で「今日の配信メール」を体験するデモ |
| `mail-sample.html` / `mail-sample-free.html` | 配信メールのデザイン見本（HTMLメール実装の見た目目標） |
| `privacy.html` / `terms.html` / `tokushoho.html` | 各種規約（法務の正本は `privacy.html`） |
| `ad-*.html` | 広告用の縦型クリエイティブ |
| `bazi.js` / `engine.js` / `daily-engine.js` | **四柱推命エンジン（変更禁止・§4）** |

### 運気予報（unkiyoho.jp）
`tenki_only/` に相当。`index.html`（=LP）／`register.html`／`mypage.html`／`my-tenki-demo.html`／`mail-sample*.html`／`zukan.html`／規約類／`bazi.js`・`engine.js`・`daily-engine.js`。**エンジンは相性診断と同一ファイル**。

> ⚠️ 相性診断の `index.html` は「相性診断本体」、運気予報の `index.html` は「運気予報LP」で**中身が別物**。取り違え注意。

---

## 4. 四柱推命エンジン（変更禁止）

3つのJSが計算の正本。**DOM非依存**なので、サーバー（Node.js）でもそのまま `require` して毎朝バッチに流用できる（移植・書き直し不要）。

| ファイル | 役割 | グローバル |
|---|---|---|
| `engine.js` | 暦（ユリウス通日）・日柱算出・干支関係・十神・十二運・桃花・相性/日運の素点・決定論シード | `window.KEngine` |
| `bazi.js` | 命式プロフィール（喜神/忌神・扶抑・24節気の節入り） | `window.Bazi` / `window.PersonBazi` |
| `daily-engine.js` | 毎朝メール文面の組み立て・日運グレード・こよみ・流年流月補正 | `window.KDaily` |

### 4.1 命式生成のシグネチャ（最重要）
```
PersonBazi(y, m, d, hour, minute, timeUnknown, sex)
```
- **時刻あり＝4柱**、`timeUnknown=true`＝**時柱を外し3柱**（日柱＝本人は常に含む）。
- **毎朝メール生成時に、登録済みの `birthTime`／`timeUnknown` を必ず渡すこと。**渡し忘れると全会員が3柱になり「四柱推命」の看板と実態がズレる。
- 時刻が効く範囲：身強弱・喜神/忌神 → 本質カラー・毎日のラッキー・追い風判定・💮◎〇△。効かない範囲：🌈🌸等の日柱ベースの縁マーク。

### 4.2 暦のアンカー（検証済み・変更禁止）
`engine.js`：`2000-01-01 = 戊午`（`ANCHOR_INDEX=54`）。検証点：`2008-03-28=丁卯` / `1981-06-10=己未`。日柱は `((ANCHOR_INDEX + (jdn(y,m,d)-ANCHOR_JDN)) % 60 + 60) % 60`。

---

## 5. 相性スコア仕様（相性診断）

**四柱推命に本来「点数」は無い＝当サービス独自指標。** 以下は `index.html` の `pairPattern` の確定実装。

### 5.1 素点テーブル（変更禁止）
```
天干関係 ST_PTS = {干合:30, 干相生:19, 干比和:12, 干相剋:6}
地支関係 BR_PTS = {支合:28, 三合:23, 同:20, 支相生:18, 支比和:14, 支相剋:8, 破:5, 害:3, 刑:2, 沖:0}
```

### 5.2 計算式
```
score = clampScore(42 + BR_PTS[branchRel] + ST_PTS[stemRel])
clampScore(v) = max(45, min(97, v))          // 通常スコアは 45〜97 に収まる
```
- **十二運による微調整**：`clampScore(score + round((pairJuni.base - 80) / 2.5))`（±数点の小さな補正。ここは意図的に小さくしてある。倍率を上げると地支の凶作用より十二運が勝ってしまうので**変えない**）。

### 5.3 特別縁（固定スコア・演出あり）
| 条件 | 判定名 | スコア | 表示 |
|---|---|---|---|
| 干合 **かつ** 支合（=1/60・約1.66%） | **天地徳合** `tokugou` | **100** | 「運命で結ばれた、奇跡の二人」🎉 |
| 干支が完全に同じ | **律音** `ritsuin` | **88** | 「うり二つの、似た者どうし」 |

### 5.4 スコア帯の意味（表示ラベル）
運命度メーターの帯：`縁が薄い` → `普通` → `深い縁` → `天地徳合`（右端）。

### 5.5 既知の設計判断（バグではない）
干合（天干の強い結びつき＝+30）を持つペアは、地支が凶（沖・害等）でも、地支だけ良い別ペアよりスコアが高く出ることがある。**これは意図した設計**（強い干合が弱い地支の凶を上回る＝四柱推命的に妥当）。単独診断の表示は常に正しい。旧TODOの「逆転」懸念（十二運90%云々）は**旧スコア方式の話で、現行§5.2では解消済み**。

---

## 6. 日運・運命カレンダー仕様（運気予報・マイページ）

### 6.1 グレード判定（💮◎〇△）— **相対評価**
本人の60干支スコア分布から閾値を決め、誰もが「自分の中の良い日／静かな日」を持つ。皆と同じカレンダーにならない。**`mypage.html` / `my-tenki-demo.html` / メール（`daily-engine.js`）で判定順が完全一致していること。**

日ごとの素点（`_dayRaw`）：
```
BASE(十神) = {正官:2, 正財:1.8, 偏財:1.6, 食神:1.4, 印綬:1.0, 比肩:0.6, 偏印:0.2, 傷官:-0.8, 劫財:-1.0, 偏官:-2.0}
+ 喜神(追い風) +1.2 / 忌神(整える) -1.2
+ 沖(ゆらぎ) -2.0
+ 流年流月補正 ymAdjust（後述、YM_SCALE=0.6倍）
```
閾値（本人の60日分布から）：`◎ = 上位約28%（72パーセンタイル）` / `△ = 下位約15%`。

判定順（この順序を変えない）：
1. 🌈（運命の人の日）→ **💮**
2. 桃花 かつ 非沖 かつ 上位28% → **💮**
3. 上位28% → **◎**
4. 恋の合図日 かつ 非沖 かつ 中位以上 → **◎**（底上げ）
5. 下位15% → **△**
6. それ以外 → **〇**

### 6.2 グレードの表示ラベル（UI文言・確定）
| 記号 | ラベル |
|---|---|
| 💮 | 特別な日 |
| ◎ | 調子のいい日 |
| 〇 | 普通の日 |
| △ | **無理しない日**（旧「充電日」から変更済み。⚠️注意喚起の意図） |

### 6.3 流年流月補正 `ymAdjust`（`daily-engine.js`）
- 年柱・月柱は `bazi.js` の正確な節入り（立春・24節気）で取得。
- 重み：月干±0.25／月支±0.15／年干±0.18／年支±0.12（合計最大±0.7）。**`YM_SCALE=0.6` 倍で適用中。この1か所が3画面すべての正本。**倍率を変えると◎/△分布が全画面連動で変わる。
- 中庸（喜も忌も無い）の人は日主との生剋で判定 → 中庸でもカレンダーに山谷が出る。

### 6.4 カレンダーの表示範囲（確定仕様）
- 登録会員：**今月＋来月の2か月のみ**閲覧可。範囲外へ進もうとすると案内表示。
- 体験デモ（`my-tenki-demo.html`）：**当月のみ**。前後を押すと案内表示。

---

## 7. データの正本（60干支）

`index.html` 内に定義。**下記の依存関係を守り、片方向に同期する**。

| データ | 内容 | 正本/追従 |
|---|---|---|
| `TYPE_NAME` | 60干支のタイプ名（例：己未=「円満づくりの名人」） | **正本** |
| `KANSHI_TAGS` | 干支ごとのハッシュタグ配列（末尾＝弱み系タグ） | **正本** |
| `MASCOT_INFO` | `[タイプ名(予備), 説明文]`。**タイプ名表示は必ず `TYPE_NAME` を優先**し、`MASCOT_INFO[0]` はフォールバックのみ（現状は同期済み・全一致） | 追従 |
| `zukan.html` の各 `tn` | 図鑑のタイプ名 | `TYPE_NAME` に追従 |

- コード上：`var tn = TYPE_NAME[stem+branch] ? TYPE_NAME[stem+branch] : MASCOT_INFO[key][0]`。
- **タイプ名を変えるときは `TYPE_NAME` を直し、`zukan.html` の `tn` と `MASCOT_INFO[0]` も揃える**（3か所）。揃え漏れがあると「図鑑とタイトルのズレ」になる。
- CORE / PAT オブジェクト：相性パターン別の本文（見出し h・説明 d・関係性 r 等）と、`k`＝仲直りのコツ（すれ違い時）を持つ。ユーザー可視テキストに**アスタリスク（*）を入れない**方針（AI感を出さないため）。

---

## 8. 相性診断の登録ゲート（166・実装済み）

無料で遊べるが、一定回数で登録を促す。**カウントと壁判定は診断ボタン `#go` の `onclick` に実装**（`index.html` 末尾の `<script>`）。

```
DIAG_KEY = "enbiyori_diag_count"   // localStorage
FREE     = 4                        // 無料回数
```
- 挙動：未登録 かつ 非デモバイパス かつ `diagCount >= 4` のとき **登録壁 `#regWall` を開く**。診断成功後に `diagCount` を+1。
  - 1〜4回目＝結果表示、**5回目＝登録壁**。（実測確認済み）
- 登録判定：`enbiyori_register` に1件以上あれば無制限。
- **デモバイパス**（`_demoBypass`）：`enbiyori_diag_bypass="1"` で回数制限を無視。**本番版ではこの関数を `return false` に固定し、デモボタンの`<script>`ブロックごと削除済み**。本番に復活させないこと。

---

## 9. localStorage キー一覧

| キー | 用途 | 備考 |
|---|---|---|
| `enbiyori_diag_count` | 相性診断の利用回数 | 登録ゲート（§8） |
| `enbiyori_diag_bypass` | デモ用バイパス | **本番では未使用**（デモ削除済み） |
| `enbiyori_register` | 登録済み情報（プロトタイプ） | 1件以上でゲート解除 |
| `enbiyori_self_birth` | 本人の生年月日等プロフィール | マイページ・デモの命式生成に使用 |
| `enbiyori_ref` | 流入リファラ等 | 計測用 |
| `enbiyori_memo_YYYY-M-D` | カレンダーの日別メモ | マイページ |

> プロトタイプはlocalStorageで完結。バックエンド導入時は、これらを**サーバーAPI（登録・ログイン/引き継ぎ・配信設定）へ置き換える**。マイページの表示ロジックは会員オブジェクト1つ（`loadSelf()`）に依存しているので、そこをAPIフェッチに差し替えれば済む設計。

---

## 10. 運気予報のバックエンド（要実装）

### 10.1 登録受付
- `register.html` の収集項目：ニックネーム`nick`／メール`email`／生年月日`birth`(YYYY-M-D)／生まれた時間`birthTime`(HH:MM・任意)／時刻不明`timeUnknown`／性別`sex`／今の状況`rel`(single/crush/partner/married/work)／都道府県`pref`／関心タグ`interests[]`／各種同意＋**同意日時 `consent_at`**／18歳未満は**保護者同意 `minorConsent`**。
- **現状の送信先はプレースホルダ**（§12）。自前受信API化を推奨（重複送信対策・二重オプトイン確認メール）。
- ユーザー入力は本文・ページ挿入時に**必ずHTMLエスケープ**（XSS）。

### 10.2 毎朝の鑑定メール
- バッチ：**毎朝 JST 6:00 までに全会員分を生成・送信**（「起きたらもう届いている」）。
- 生成ロジック＝`my-tenki-demo.html` の `run()` が組み立てる本文と**完全に同一**（`daily-engine.js` を Node で流用）。
- 契約 API：`generateDaily(birth, {..., personBazi})`。返却に `todayOne`（その日いちばんの具体アクション1つ／「✅ 今日はこれだけ」枠に表示）と `koyomi.items`（こよみマーク）を含む。**`personBazi` を必ず渡す**（§11）。
- **HTMLメール化の注意**：見本はWeb用CSS（grid等）。実配信は**テーブルレイアウト＋インラインCSS**へ移植（Gmail等がCSSを削るため）。見た目の目標は `mail-sample.html`。
- **法定フッター（特定電子メール法・必須）**：機能する**配信停止URL**／設定変更（マイページ）リンク／事業者名・住所。見本のダミーリンクを本番で差し替え。
- **全メール共通**：末尾に「💞 1.66相性診断への導線」＋シェアボタン（LINE/X）ブロックを入れる（見本の `data-aishou` ブロックが正）。`example.com` を本番URLへ。
- 件名例：`【7/9(木)】あかりさんの今日の運勢🌤`。

### 10.3 配信停止（オプトアウト・必須）
- 受信者ごとに**解除トークン付きURL**を発行し、各メールの「配信停止」リンクに設定。ワンクリックで確実に停止（ログイン不要が望ましい）。
- マイページの「毎朝の予報メール」トグル（`#swMail`）の状態をサーバー保存し、配信基盤と同期。

### 10.4 LINE配信（任意・希望者のみ）
- 会員種別の区別なし。`lineOptIn`（登録）／`swLine` トグル（マイページ・デフォルトOFF）で希望した人にだけ、LINE Messaging API で毎朝Push。友だち追加者のみ。

### 10.5 同意・広告
- 配信同意・広告目的の属性利用への同意を**同意日時つきで永続保存**（特電法・個情法）。
- 広告枠（メール内・サイト内）には「**広告/PR**」の識別表示。
- 18歳未満：保護者同意を確認し、**属性を広告ターゲティング・第三者提供に使わない**分岐。

### 10.6 データモデル（推奨最小・課金項目は撤去）
```
users:   id, email(uniq), nick, birth_date, birth_time(null可), time_unknown(bool),
         sex, pref, rel, interests(json), line_opt_in(bool), line_user_id(null可),
         minor_consent(bool), consent_at(datetime), mail_opt(bool), remind_opt(bool),
         created_at, updated_at
mail_log:   id, user_id, date, channel(email/line), status(sent/bounce/fail), sent_at
unsub_token: user_id, token   // ワンクリック配信停止用
```
- 生年月日・時刻は**要配慮に準じる個人情報**として保存時暗号化・アクセス制御を厳格に。配信同意・解約の記録は消さない。

---

## 11. カレンダー×メール一致の「必須契約」（実装時に厳守）

グレード判定コードは3画面（メール `daily-engine.js:dailyGrade` ／ マイページ `mypage.html:dayGrade` ／ デモ `my-tenki-demo.html:dayGrade`）で一致済み。**メールを本当に一致させるには配信バッチ側で以下を守る：**

1. **`generateDaily(birth, {..., personBazi})` に `personBazi` を必ず渡す。** 渡さないと喜神/忌神が空（中庸扱い）になり、favSet・忌神・`ymAdjust`・素点がズレて、喜忌を持つ大多数の人で「メール〇／カレンダー◎」の不一致が出る。`personBazi` は**カレンダー側と同一バージョンの `bazi.js`／`PersonBazi`** で生成（別実装・別バージョン禁止）。
2. **神殺・冲マークは `generateDaily().koyomi.items` を描画する**（カレンダーと同一表引き）。年月柱冲🌀は `personBazi` がある時のみ算出。
3. **`YM_SCALE=0.6` を変えない**（変えると◎/△分布が全画面連動で変わる）。
4. **「今日」はJST基準**（メールもJST配信で揃える）。
5. 推奨：CIで「同一 birth → 同一 grade/idx」をメール側とカレンダー側で突合する契約テストを置く。

> 品質実測（2026-07）：同一人物120日連続生成でメール完全一致0件・前日一致0/12。10人×365日でカレンダーの神殺集合とメールが100%一致。**文面プール長・seed計算を改変しないこと**（暦周期と互いに素に設計してある）。

---

## 12. 公開前の差し替え（プレースホルダ）

| 項目 | 現状 | やること |
|---|---|---|
| 登録フォーム送信先 | `register.html` の `GFORM.action` に `"FORM_ID"` を含む間は**POSTせずlocalStorage保存のみ**（＝どこにも送信されない） | 実フォーム/自前APIを用意し `entry.*` と `action` を実値へ。`consent_at`・各同意・`minor_consent` を必ず保存。パスワードは送らない/平文で貯めない |
| OGP | `og:url` が `example.com`／`ogp-*.png` がダミー | 本番ドメイン・実画像に差し替え。**OGP画像はPNG/JPGのまま**（SNS互換） |
| メール導線URL | 見本内 `example.com` | 本番の相性診断/運気予報URLへ（シェア用エンコードURLも） |
| 星評価・件数・お客様の声 | `display:none`（実データ待ち） | 実データが揃ったら表示に戻す（index/lp/register） |
| 特商法 | `tokushoho.html`：無料・非販売のため販売条項対象外。所在地・代表者は「請求時開示」運用 | 広告出稿段階では、審査要件として**所在地・代表者名**の掲載準備（法務確認のうえ） |
| Googleカレンダー自動連携 | `gconnDemo()`（準備中alert）。日別「📅追加」は実URLで稼働中 | 必要なら Google Calendar API(OAuth) で実装。当面は「準備中」表示のまま可 |

---

## 13. やってはいけないこと（禁止リスト）

- ❌ `bazi.js`／`engine.js`／`daily-engine.js` の計算・定数・シード・プール長の改変。
- ❌ §5 スコア定数（ST_PTS/BR_PTS/clamp/特別値）、§6 グレード定数・判定順・`YM_SCALE`、の変更。
- ❌ 毎朝メールを別ロジック/別バージョンのエンジンで生成すること（カレンダーとズレる）。
- ❌ `TYPE_NAME` だけ直して `zukan.html`／`MASCOT_INFO` を放置（図鑑とズレる）。
- ❌ 本番へデモ解除ボタン／`_demoBypass` を復活させること。
- ❌ ユーザー可視テキストにアスタリスク（*）を入れること（AI感）。
- ❌ 会員種別（無料/有料）の分岐や決済フローを実装すること（現行は全員フル・無料）。
- ❌ UTCで「今日」を判定すること（JST固定）。

---

## 14. 実装タスク推奨順（運気予報のバックエンド）

1. 登録API（`register.html` の送信先差し替え）＋同意記録の永続化。
2. 毎朝バッチ：`daily-engine.js` を Node で流用 → `personBazi` を渡して生成 → HTMLメール（テーブル化）→ SES送信。
3. 配信停止（解除トークンURL）＋マイページのメールトグル同期。
4. マイページAPI接続（会員情報・配信設定）。
5. LINE：友だち追加案内 → 希望者Push。
6. 広告枠・属性活用の同意分岐（18歳未満の除外）。
7. 法務チェック（特商法/プライバシー）→ OGP・URL差し替え → 本番ドメイン切替。

---

### 付録：クイックリファレンス（現行v.0824-226 実測値）
- 相性スコア範囲：通常 45〜97／律音 88／天地徳合 100。
- 登録ゲート：無料4回→5回目POP（`enbiyori_diag_count`）。
- カレンダー：本人分布で💮◎〇△（◎上位28%・△下位15%）、今月＋来月のみ。
- タイプ名の正本：`index.html` `TYPE_NAME`（図鑑・MASCOT予備は同期済み・ズレ0）。
- アスタリスク：全ページ表示テキストに混入なし（確認済み）。
