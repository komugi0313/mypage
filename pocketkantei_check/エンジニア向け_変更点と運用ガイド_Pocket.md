# Pocket鑑定 2026-09-25 変更点と運用ガイド（エンジニア向け）

この資料は、2026-09-25 の改修内容を、実装・デプロイ・運用するエンジニアが正しく理解するためのものです。
既存の `docs/Pocket鑑定_技術仕様書.md` のうち、この資料と食い違う部分はこの資料が優先です（仕様書側にも注記を入れてあります）。
`docs/技術仕様書.html` は 2026-08-21 時点の古い版のままです。最新は Markdown 版（`Pocket鑑定_技術仕様書.md`）とこの資料です。

---

## 0. 3行まとめ
1. **命式エンジンの修正**：節入り判定が北京時間基準で1時間ずれていたのを修正しました。大運は必ず「0歳〜立運＝月柱」から始まる形でデータ化し、年運は立春で切り替えます。
2. **アカウント機能の新設**：メールアドレス＋パスワードで登録・ログインでき、データはサーバーに自動保存されます。確認メールのリンクは使いません。パスワードは、利用者が再設定メールで自分で再設定できます。
3. **デプロイ前に環境変数が3つ増えます**：`AUTH_SECRET`（必須）、`RESEND_API_KEY` と `MAIL_FROM`（パスワード再設定に必須）。

---

## 1. デプロイ前チェックリスト

| # | 作業 | 必須度 | 未設定時の症状（画面の表示） |
|---|---|---|---|
| 1 | Netlify 環境変数 `GEMINI_API_KEY`（既存） | 必須 | 鑑定チャットが動かない |
| 2 | Netlify 環境変数 **`AUTH_SECRET`**（32文字以上のランダム文字列。例：`openssl rand -hex 32`） | **必須** | 登録・ログイン不可「ただいまアカウント機能が使えません（NO_SECRET）」 |
| 3 | [Resend](https://resend.com) に登録し、送信元ドメイン（例：72k.ai）を DNS で認証して APIキーを発行 | パスワード再設定に必須 | ― |
| 4 | Netlify 環境変数 **`RESEND_API_KEY`**（上のAPIキー）と **`MAIL_FROM`**（例：`Pocket Kantei <no-reply@72k.ai>`。認証済みドメインのアドレス） | パスワード再設定に必須 | 「パスワードを忘れた方」で「…（NO_MAIL）」 |
| 5 | **Git連携 または `netlify deploy`（CLI）でデプロイ**。管理画面へのzipドラッグ＆ドロップは不可 | **必須** | 関数・Blobsが動かず「（HTTP404）」「（NO_STORE）」 |
| 6 | `package.json` の依存 `@netlify/blobs`（^8.1.0）が関数のビルドでインストールされること | 必須 | 「（NO_STORE）」 |

- **`AUTH_SECRET` は一度決めたら変えないでください。** 変えると全員のログイン状態（トークン）が無効になり、再ログインが必要になります。データは消えません。
- **料金について：** 送信数の上限は **Resend 側の料金プラン** によるものです。アプリに回数制限を作ったわけではありません。Resend の無料プランは送信数に上限があります（目安として1日100通・月3,000通。**最新の料金は Resend 公式サイトで確認してください**）。利用者が増えて上限を超えると、その日は再設定メールが送れなくなるので、有料プランへの切り替えを検討してください。
- **更新の配信：** Service Worker のキャッシュ版は `pocket-kantei-v5` です。以後も更新時には `sw.js` の `CACHE` を上げてください。

### 画面に出るエラーコードの意味（アカウント関連）
画面の「ただいまアカウント機能が使えません（XXX）」の XXX が原因の目印です。

| コード | 原因 | 対処 |
|---|---|---|
| `NO_SECRET` | `AUTH_SECRET` 未設定、または16文字未満 | 環境変数を設定して再デプロイ |
| `NO_STORE` | Netlify Blobs に接続できない | Git連携／CLIでデプロイ、`@netlify/blobs` のインストール確認 |
| `HTTP404` | `/api/auth` が見つからない（関数未デプロイ、`netlify.toml` のリダイレクト未反映） | `functions/auth.js` と `netlify.toml` を含めて再デプロイ |
| `NO_MAIL` | `RESEND_API_KEY` または `MAIL_FROM` 未設定 | 環境変数を設定 |
| `MAIL_FAIL` | Resend への送信失敗（ドメイン未認証・APIキー誤り・上限超過など） | Resend の管理画面のログを確認 |
| `SERVER` | その他のサーバーエラー | Netlify の Functions ログを確認 |

---

## 2. 変更ファイル一覧

| ファイル | 種別 | 内容 |
|---|---|---|
| `pro-bazi.js` | 修正 | **9/26：PRO v184 のエンジンを土台に置き換え**（Pocket独自は `stdMer` 基準の経度補正と `lunarToSolar` のみ）。節入り判定の時差修正（`BJ2`）、年運の開始年を立春基準に |
| `pro-adapter.js` | 修正 | 出生地の標準子午線を常に渡す、第一運＝月柱（`monthDecade`/`curDecade`）、今年＝立春基準（`now.year`/`now.calYear`）、AI用データの大運表記、9/26：`fukyuList`・`gogyoBalance` を v184 版に、大運の西暦（`decadeYears`） |
| `index.html` | 修正 | 大運表示（月柱・◯歳◯ヶ月）、AIへの指示文、節木運、アカウント画面・同期、パスワード表示ボタン、LPへの振り分け、9/26：出生地のIANAタイムゾーン（`tz`/`tzOffsetMin`）、AI回答の言語・年号・専門語の対策 |
| `functions/auth.js` | **新規** | アカウントAPI（登録・ログイン・保存・読込・パスワード変更・削除・再設定メール・再設定） |
| `functions/gemini.js` | 修正 | Netlify Blobs 接続の不具合修正（`connectLambda`） |
| `netlify.toml` | 修正 | `/api/auth` → `/.netlify/functions/auth` のリダイレクトを追加 |
| `lp.html` | 修正 | 「はじめる」の遷移先を `index.html?start=1` に変更、「ログイン」リンクを追加 |
| `legal.html` | 修正 | プライバシーポリシー（日英）と利用規約第3条（日英）をアカウント機能に合わせて改定 |
| `sw.js` | 修正 | キャッシュ版を v8 に（9/26 の修正で v5→v8）、`/api/` を Service Worker の対象外に |
| `README_デプロイ.md` | 修正 | 環境変数の追記 |

---

## 3. 四柱推命エンジンの修正（`pro-bazi.js` / `pro-adapter.js`）

### 3.1 節入り判定の時差（不具合修正）
- **原因：** 暦ライブラリ tyme4ts の節気時刻は **北京時間（UTC+8）** 基準です。エンジンは日本時間（や各国の現地時刻）をそのまま比べていたため、日本生まれなら節入りの1時間前に生まれた人の年柱・月柱・順逆・大運がずれていました（ランダム2万件中44〜45件、約0.2%）。米国東部生まれなら13時間分ずれます。立運の日数計算だけは `-1/24` で日本時間に合わせてありましたが、命式そのものはずれていました。
- **修正：** `BJ2(r)` で出生の瞬間を北京時間の時計に換算します（`(120 − 標準子午線) × 4分` ずらす。`r.stdMer` が無ければ日本 135° とみなす）。
  - **年柱・月柱・順逆・立運・大運：** 換算した時刻で判定
  - **日柱・時柱：** 従来どおり現地の時刻（真太陽時）で判定
  - `W2` の `-1/24` は削除（換算を `BJ2` に一本化）
- `pro-adapter.js` は、出生地の標準子午線（`place.mer`）を **時刻不明の場合も** `stdMer` として渡します（経度補正は時刻が分かる時だけ）。
- 真太陽時補正 `Q2` の分の加減算を、ブラウザの地域設定・夏時間に左右されない UTC 計算にしました。
- **検証：** 日本生まれ2万件で、変わったのは節入り直前の45件だけでした。変わった例を独立した天文計算（ephem）の節入り時刻と照合し、修正後が正しいことを確認しています。

### 3.2 大運のデータ構造（ルール：大運は必ず0歳の月柱から）
- エンジンの `decadeFortunes[]`（8本）は **立運以降の10年運だけ** です（添字 0 ＝ 月柱の次の干支）。**この配列の添字は変えていません。**
- 第一運（0歳〜立運＝月柱）は別オブジェクト `pro.monthDecade` で持ちます。
  - `{ isMonth:true, startAge:0, months:0, endAge, endMonths, startYear, ganzhi, stem, branch, stemElement, branchElement, tenStar, terrain }`
- **今の大運** は `curDecade(pro)`（`window.curDecade`）で取得します。立運前（`now.daeunIndex === -1`）なら月柱を返します。
  - `daeunIndex >= 0 ? decadeFortunes[i] : monthDecade` を直接書かないでください。以前 `:0`（第1大運）にしていた箇所が不具合の原因でした。
- **年齢の表示：** 立運の端数月まで出します（例：立運0歳8ヶ月 → 第一運 0歳〜0歳8ヶ月、第1大運 0歳8ヶ月〜10歳8ヶ月）。`index.html` の `_duSpan / _duSpanJ / _duSpanE` を使います。
- **「今その大運にいるか」：** 年単位の `now.age` ではなく、月単位の `now.ageMonths` で判定します。
- **立運の計算式は変更していません：** `round(節入りまでの日数 ÷ 節と節の間の日数 × 120ヶ月)` の比例方式です。一般的な「3日＝1年」とは最大2ヶ月ほど差が出ます。流派としての扱いは運営に確認済みではないため、変える場合は運営判断が必要です。

### 3.3 年運（ルール：立春で切り替え）
- `annualFortunes[]` は西暦年をキーにした干支（その年の立春〜翌立春）です。一覧は **生まれた時の年柱（立春基準）の年から** 始まります（1/1〜立春前の生まれは前年から）。
- `now.year` は **立春基準の「今年」** で、1/1〜立春前は前年になります。暦の年は `now.calYear` です。
  - 年運を引く時は必ず `now.year` を使ってください。画面に「今日の日付」として出す時は `calYear` を使います。

### 3.4 節木運（せつぼくうん）※定義は運営確認済み
- **定義：** 大運の季節（方合：亥子丑＝冬、寅卯辰＝春、巳午未＝夏、申酉戌＝秋）が変わる時。約30年ごとです。**大運の節目（10年ごと）とは別物** として扱います。
- 第一運（月柱）も大運の一つなので、月柱と第1大運の季節が違えば、立運の時点も節木運です。
- 実装は `setsubokuAnalysis()`（`index.html`）です。「今」「次の節木運」「前後1年半（自分から話題にしてよい時期）」は `now.ageMonths` で月単位に判定します。
- 喜神の季節なら「発展の30年」、忌神なら「備えの30年」と読み、十二運が墓・絶・胎の大運や空亡の大運は「強まる」扱いにしています（従来どおり）。

---

## 4. アカウント機能（新設）

### 4.1 利用者から見た動き
| 場面 | 動き |
|---|---|
| 未登録で開く | 毎回 `lp.html` を表示（以前はLP既読の印 `pk_seen_lp` で登録画面に直行していた） |
| LP「はじめる」 | `index.html?start=1` → 登録画面。命式の入力＋メール・パスワード（8文字以上・確認つき）。**確認メールのリンクは使わず即登録** |
| LP「ログイン」／登録画面下のリンク | `index.html#login` → ログイン画面。成功するとサーバーのデータで端末を置き換える |
| パスワードを忘れた | ログイン画面 → 「パスワードを忘れた方」→ メールアドレス入力 → 再設定メール（表示言語で送信・10言語）→ `index.html#reset=<uid>.<code>` → 新しいパスワード → 自動ログイン |
| 通常利用 | ログイン状態は端末に保持し、次回は自動ログイン。データは自動でサーバーへ保存 |
| メニュー「データの引き継ぎ・バックアップ」 | 先頭にアカウント欄：今すぐ保存／パスワード変更／ログアウト／アカウント削除。未ログインの既存ユーザーは、ここからアカウントを作って今のデータを移せる |
| メニュー「退会する」 | ログイン中は「アカウント削除」（パスワード確認つき）へ案内。サーバーのデータも消す |
| パスワード欄 | すべてに「表示／非表示」の目のボタン。送信時は自動で非表示に戻す |

### 4.2 API：`POST /api/auth`（`functions/auth.js`）
共通：JSON `{ action, ... }` → `{ ok:true, ... }` または `{ ok:false, code }`。他サイトからの呼び出し（Origin不一致）は 403。

| action | 入力 | 出力 | 備考 |
|---|---|---|---|
| `register` | `email, password, data` | `token, email` | 既存なら 409 `EXISTS`。同一IPから1日20件まで |
| `login` | `email, password` | `token, email, data, updated` | 5回続けて失敗すると15分ロック（429 `LOCKED`）。未登録アドレスでも同じ 401 `BAD_LOGIN` |
| `save` | `token, data` | `updated` | データ全体を上書き保存 |
| `load` | `token` | `email, data, updated` | |
| `change` | `token, password, newPassword` | `token` | `ver` が上がり、他端末のトークンは無効になる |
| `delete` | `token, password` | ― | アカウントとデータをサーバーから削除 |
| `forgot` | `email, lang` | ― | 登録の有無にかかわらず `ok:true`。1アカウント1時間3通・同一IP1日30件まで |
| `reset` | `code, newPassword` | `token, email, data, updated` | リンクは30分有効・1回限り。`ver` が上がる |

### 4.3 保存先とデータ形式（Netlify Blobs ストア `pk-accounts`）
| キー | 中身 |
|---|---|
| `u:<sha256(小文字メール)>` | `{ email, salt, hash, ver, created, updated, fails, lockUntil, data, reset:{h,exp}, resetSent:[時刻] }` |
| `r:<JST日付>:<IP>` | その日の新規登録数 |
| `f:<JST日付>:<IP>` | その日の再設定依頼数 |

- **パスワード：** `scrypt(N=16384, r=8, p=1, 64byte)`＋ランダムなソルト。平文は保存しません。
- **トークン：** `<uid>.<ver>.<有効期限>.<HMAC-SHA256(AUTH_SECRET)>`（365日）。`ver` が一致しなければ無効です。
- **再設定コード：** ランダム24バイトを送信し、サーバーには sha256 だけを保存します。
- **`data`：** 端末の `pk_*` キーの文字列です（上限 1,500,000 文字）。ただし次のキーは送らず、サーバー側でも除外します。
  - `pk_auth` / `pk_sync_at` / `pk_did` / `pk_key` / `pk_svdraft` / `pk_svq`

### 4.4 端末側（`index.html`）
- 新しい localStorage キー：
  - `pk_auth`：`{ email, token }`。**パスワードは保存しません**（パスワードはブラウザのパスワード保存機能に任せる）
  - `pk_sync_at`：最後に同期したサーバーの `updated`
- **自動保存：** `saveProfile` / `saveMsgs` をラップして4秒後にまとめて保存し、アプリを閉じる時（`visibilitychange: hidden`）にも保存します。
- **起動時：** サーバーの `updated` が `pk_sync_at` より新しければ（他の端末で更新されていれば）取り込んで再読み込みします。
- **ログアウト：** 最後に保存してから、端末のデータを消します（言語・配色・LP既読は残す）。その後ログイン画面へ移ります。
- **振り分け（冒頭のスクリプト＋`boot`）：**
  - プロフィールあり → チャット画面
  - `#reset=` → 再設定画面
  - `#login` → ログイン画面
  - `?start=1` → 登録画面
  - それ以外 → `lp.html`

### 4.5 既知の制約（運営判断・今後の課題）
- **登録時に確認メールを送りません**（運営の方針）。そのため、打ち間違えたアドレスや他人のアドレスでも登録できます。その場合、再設定メールは届きません。
- **同期は「最後に保存した方が勝つ」方式で、データ全体を上書きします。** 2台で同時に使うと、片方の変更が上書きされることがあります。
- ログイン用トークンは localStorage にあるため、XSS があれば盗まれる可能性があります（一般的なSPAと同じ前提）。外部スクリプトを増やす時は注意してください。
- 日次カウンター（`r:` と `f:`）は自動では消えません。量が増えたら、定期削除を検討してください。
- メールの本文の見た目は簡易なHTMLです。ブランドに合わせる場合は `auth.js` の `sendResetMail` と `MAIL_TEXT` を編集してください。

---

## 5. その他の修正
- **`functions/gemini.js` の Blobs 接続：** Lambda互換（`exports.handler`）の関数では、Blobs を使う前に `connectLambda(event)` が必要です。これが無かったため、**本番で利用回数の制限（レート制限）が効いていなかった可能性があります。** 修正済みです。
- **法務（`legal.html`）：** 次の点を反映し、最終改定日を2026年9月25日にしました。
  - プライバシーポリシー（日英）：保存場所（端末＋アカウント）、アカウント情報（パスワードはハッシュのみ）、利用目的、第三者送信（メール配信事業者 Resend へメールアドレスのみ）、安全管理、削除方法
  - 利用規約第3条（日英）：アカウントとパスワードの自己管理
- **登録画面の文言：** 「当社サーバーに保存されません」を、10言語すべてで実態に合わせて書き換えました。

---

## 6. 検証内容（再現用スクリプトは `pocketkantei_check/tests/`）
| 検証 | 結果 |
|---|---|
| 大運の一括チェック 1万件（1番目＝月柱・期間0歳〜立運／2番目＝月柱±1で立運から／以降10年ごと） | 不一致0件 |
| 節入り修正の前後比較 2万件 | 変化は節入り直前の45件のみ（修正後が正しい） |
| 立運の照合（1972-10-26 08:00 女性 東京） | 立運5歳11ヶ月・逆行で一致 |
| アカウントAPIの単体テスト（登録・重複・形式・ログイン・ロック・保存・読込・偽造トークン・変更・削除・Origin） | 全件期待どおり |
| 画面の通しテスト（Chromium・2台の端末を想定：登録→別端末ログイン→ログアウト→既存ユーザーの作成→変更→退会案内→削除） | エラー0件 |
| 再設定メールの通しテスト（メール送信は仮のものに差し替え） | 未登録には送らない／リンクで再設定→データ復元／リンク再利用・旧パスワード・旧端末は無効 |
| LPへの振り分け（未登録で1回目・2回目・ホーム画面アイコン→LP、LP「はじめる」→登録、「ログイン」→ログイン、登録済み→チャット） | 全件期待どおり |
| （9/26）v184 エンジンとの比較（日本135°）／旧 Pocket エンジンとの比較 | 1667件・2万件とも不一致0件 |
| （9/26）時差：全出生地×ランダム日時 6240件を独立計算（ephem・zoneinfo）と照合 | 不一致0件 |
| （9/26）AI回答の後処理の単体テスト（`unitguard.js`） | 全件期待どおり |
| （9/26）実際の Gemini で10言語ランダム会話（`live.js`＋`livecheck.py`、4巡） | 最終巡（4巡目）は10言語すべてで、自動チェックの問題0件（言語混入・仏暦・星座・専門語・提案ボタン漏れ・連続読点）。返事の西暦はすべて正しい |

**本番の Netlify 環境（実際の Blobs・Resend）での動作確認はまだです。** デプロイ後に、次の流れを実機で確認してください。
- 登録 → 別端末でログイン
- パスワードを忘れた → メールが届く → 再設定

---

## 7. 2026-09-26 追加修正：ベースエンジン v184 との整合・時差・10言語

### 7.1 エンジンを v184 に合わせた（`pro-bazi.js` / `pro-adapter.js`）
- `pro-bazi.js` は **PRO v184 のエンジンをそのまま土台** にしました。Pocket 独自の変更は次の2点だけです（ファイル先頭にも記載）。
  1. 真太陽時補正 `Q2` の経度差を、固定の 135° ではなく **`stdMer`（出生地の実効子午線）基準** で計算する。v184 は日本専用なので 135° 固定ですが、そのまま使うと海外生まれの時柱・日柱がずれます（ランダム5000件中1636件）。
     - `let e=r.longitude!=null?(r.longitude-(r.stdMer!=null?r.stdMer:135))*4:0`
  2. 旧暦入力用に `window.Bazi.lunarToSolar()` を追加。
- `pro-adapter.js` の `fukyuList` と `gogyoBalance` を v184 版に置き換えました（韓国語の文字列が混ざっていた旧版を廃止）。共通の27関数は v184 と同一です。
- `markTimeUnknown` は蔵干の役割名 `本気` と `本氣` の両方を受け付けます。
- **検証：** 日本（135°）の出生で v184 と比較して 1667件中 不一致0件、旧 Pocket エンジンとの比較で 2万件中 不一致0件（柱・通変星・十二運・蔵干・司令・立運・大運・年運・五行・元命・now すべて）。

### 7.2 時差（出生地のタイムゾーン）
- **原因：** 出生地ごとに「固定の標準子午線＋自動の夏時間」で時差を出していたため、過去の制度変更を反映できていませんでした。例：日本の夏時間（1948〜51年）、中国の夏時間（1986〜91年）、韓国の UTC+8:30 時代（1954〜61年）、英国・メキシコ・ブラジル・ロシア・シンガポール・マレーシアの標準時変更。ランダム4160件中140件で命式がずれていました。
- **修正：** 出生地（`PLACES`）に IANA タイムゾーン名（`tz`：`Asia/Tokyo`、`Asia/Seoul`、`Asia/Shanghai`、`Pacific/Honolulu` など）を持たせました。`tzOffsetMin()` が `Intl.DateTimeFormat` から **その日時の実際の UTC 差** を求め、`mer = UTC差(分) ÷ 4` として出生時刻と一緒にエンジンへ渡します。
  - `tz` が無い地点や、ブラウザが対応していない場合は、従来の方式（標準子午線＋自動夏時間）に戻ります。
- **検証：** 独立した天文計算（Python の ephem と zoneinfo で、太陽黄経から年柱・月柱、視太陽時から時柱、ユリウス日から日柱）と照合し、**全地点 6240件で不一致0件**。

### 7.3 AIの回答（10言語）
実際の Gemini（体験モードの直接キー）で10言語をランダムな出生データ・質問で3巡試し、見つかった問題を直しました。
- **西暦の誤り：** AIが大運の西暦を自分で計算して間違えることがありました。対策として、データ行に西暦を明記しました（`decadeYears()`、例：`2025/1–2035/1`、「この年を使い自分で計算しない」と指示）。
- **年号・星座のルール（`YEAR_RULE`）：** 毎回の指示と最初の挨拶に「西暦の数字だけを使う（タイの仏暦 25xx などは使わない）」「A〜B の期間は A に始まり B で次に替わる」「西洋の星座・生まれた曜日は出さない」を追加しました。
- **作り直しの条件：**
  - 返事の言語が違う（`_wrongLang`）場合は、賢いモデルで一度だけ作り直します。
  - 仏暦の年が出た場合や、簡単モードの中国語で「害」「未」などの一字の専門語やプレースホルダ（「特别的星」）が出た場合も作り直します（`_jargonLeak`）。
  - 最初の挨拶が別の言語で書かれた、または仏暦の年を含む場合は、その言語の定型の挨拶に切り替えます。
- **専門語のやわらげ（`_softReplace`）：**
  - 韓国語で単語の途中を置き換えないようにしました（例：「과정인」の「정인」）。
  - 中国語の「运势势」のような重複を解消しました。
  - 韓国語の助詞（이/가・은/는・을/를・과/와）をパッチムに合わせて直します。
- **仕上げ（`_finalTidy`）：** 連続した読点（「,,」）と末尾の読点を消します。韓国語で名前が空の時に残る「님,」も消します。
- **提案ボタンの抽出（`_extractChips`）：** `"…" / "…"` の形で本文末尾に出た質問候補を、引用内に改行があってもボタンとして回収します。
- **スペイン語・ポルトガル語：** 利用者の性別に合わせた文法（男性形・女性形）を使うよう指示しました（`_genderRule`）。
- **最初の挨拶：** 途中で切れないよう、出力の上限を 820 から 1400 トークンに増やしました。

---

## 8. 2026-09-26 追記（2）：発売前の総点検

### 8.1 修正（`index.html`）
- **`_localBirthday`：** 相性の入力（相手の誕生日）を「自分の誕生日の質問」と誤判定し、決まり文句を返していました（日本語以外の9言語）。
  - 日付（西暦4桁・`5/3` など）を含む文は対象外にしました。
  - 相手を表す語を10言語に拡充しました。
  - ログイン中は「端末の中だけに保存」の注記を外します。
  - es・pt は性別で語形を変えます。
- **`_parseDateAny`（新規）：** `chatPartnerScan` から使います。次の日付の書き方を読みます。
  - `Y年M月D日`、`Y년 M월 D일`、`Y/M/D`
  - `D tháng M năm Y`
  - 月名つき（英・西・葡・印尼・タイ語。`May 3, 1990`／`3 de mayo de 1990`／`3 Mei 1990`／`3 พฤษภาคม 2533`（仏暦は543を引く））
  - `D/M/Y`（英語は `M/D/Y`。13以上の数字があれば日と確定）
  - 誕生日を示す語に `cumplea`・`ulang tahun`・`ngày sinh`・`nascimento` を追加しました。
- **相手の性別の判定：** 10言語の「彼氏／夫／novio／anh ấy／suami／แฟนหนุ่ม」などに対応しました。判定できない時は従来どおり female です。
- **`proBlock`：** AI用データに2行を追加しました。AIが相手の誕生日を本人のものと取り違えていたためです。
  - `USER … born Y/M/D`（本人）
  - `PARTNER … born Y/M/D — NOT the user's birthday`
- **`_dropRepeatedParas`（新規）：** 返事の2段落目以降で、直近30件のAI吹き出しとの 8文字 n-gram の一致率が 0.6 以上の段落を落とします。返事全体の丸写しは、従来の `_nearDupOfHistory` が作り直しで対応します。
- **`_finalTidy`：** 中文の「你们的你的特质」「你的你的」を直します（「命盘→你的特质」の置換の副作用）。
- **相談窓口（`HELPLINE`）：** 端末のタイムゾーン → 端末の言語設定の地域 の順で国を推定します（`_deviceCountry`）。
  - その言語の既定の国（ja→JP、es→ES、en→US など）と違う国なら、`HL_LOCAL` の窓口（26か国）に差し替えます。
  - 一覧にない国では「地域の緊急番号へ」だけを出し、他国の番号は出しません。
- **マイページ：** ログイン中は「アカウント（メールアドレス）に自動保存」と表示します（`MP_CLOUD`、10言語）。
- **開発者用の体験モード欄（`#devBox`）：** 既定で非表示にしました。次のいずれかの時だけ表示します。
  - `?dev=1` で開いた（`pk_dev` を保存）
  - `pk_key` がある
  - `PK_DIRECT_KEY` がある

### 8.2 修正（`functions/gemini.js`）
- **プラン名のずれ：** アプリの `std` と `unl` がサーバーの `LIMITS` に無く、free（1日5回）扱いになっていました。`std`（80回/日）と `unl`（150回/日）を追加しました。
- **`x-pk-plan` を信用しない：** 課金を実装するまでは、全員を free として扱います。環境変数 `PK_TRUST_PLAN_HEADER=1` の時だけヘッダーを使います（テスト用）。
  - 課金の実装後は、アカウントのトークンからサーバー側で購読状態を引いて判定してください。
  - 詳しくは `pocketkantei_check/発売前チェックと課金の進め方.md` を参照してください。

### 8.3 検証
| 検証 | 結果 |
|---|---|
| 実際の Gemini で10言語×16問の長い会話（恋愛・仕事・お金・健康・家族・今日・お礼・同じ質問の2回目・詳しく・晩ごはん・落ち込み・相性・短所・来月・英語での質問・深刻な相談）（`bigtest.js`＋`bigcheck.py`） | 別の言語の混入 0／エラー・返事なし 0／前と同じ文 4件（ベトナム語の段落の写し → 8.1 で対処） |
| 登録・エラー・マイページ・各画面（10言語）（`ui10.js`） | 画面エラー 0。文言はすべてその言語 |
| 日付の読み取り（10言語＋追加例）（`datep.js`）、自分の誕生日の質問（`bday.js`）、相談窓口の国判定（`hl.js`） | 全件期待どおり |
| 8.1 の修正後の、実際の AI での再確認 | **未実施**（テスト用キーのクレジットが尽きたため）。AI用データへの反映（`pb.js`）と単体テストは確認済み |

---

## 9. 2026-09-26 追記（3）：課金（App Store / Google Play）の土台

ストアの登録が終わる前に準備できる部分を実装しました。**RevenueCat を使う前提** です。ネイティブ化（Capacitor）と、ストアの商品登録が済めばつながります。

### 9.1 仕組み
```
アプリ（Capacitor＋RevenueCat の Purchases プラグイン）
  └ 購入（appUserID ＝ アカウントの uid ＝ トークンの先頭64桁）
        ↓ ストア → RevenueCat
RevenueCat の Webhook → POST /api/billing（functions/billing.js）
        ↓ アカウント u:<uid> の rec.sub に { plan, product, expires, willRenew, store, env, eventTs } を記録
auth.js：login / load / register / reset / status の応答に plan（{plan, expires, willRenew}）を付けて返す
gemini.js：x-pk-auth（ログインのトークン）→ rec.sub → プランごとの1日の上限（LIMITS）
```
- **プランの正本はサーバーです。** アプリは、受け取った `plan` を `state.profile.plan` に反映し、表示と端末側の上限に使います（`applyServerPlan`）。
- **購入はログイン中だけ** です。機種変更しても、同じアカウントでログインすればプランが戻ります。
- **匿名での購入（`$RCAnonymousID`）の通知は無視します。**

### 9.2 `functions/billing.js`（新規）
- **認証：** `Authorization` ヘッダーが `REVENUECAT_WEBHOOK_AUTH`（または `Bearer ＋同じ値`）と一致する時だけ受け付けます。
- **通知の種類ごとの処理：**
  - 付与：`INITIAL_PURCHASE`／`RENEWAL`／`PRODUCT_CHANGE`（`new_product_id`）／`UNCANCELLATION`／`NON_RENEWING_PURCHASE`／`SUBSCRIPTION_EXTENDED`／`TEMPORARY_ENTITLEMENT_GRANT`
  - 解約（`CANCELLATION`）：期限までは使えます（`willRenew:false`）。
  - 期限切れ（`EXPIRATION`）：free に戻ります。
  - 支払いの問題（`BILLING_ISSUE`）：印を付けるだけで、ストアの猶予期間中は使えます。
  - `TRANSFER`：移動元から移動先へ、購読を付け替えます。
  - `TEST`：何もしません。
- **古い通知が後から届いた時**（`event_timestamp_ms` が記録より古い）は無視します。
- **商品ID → プラン：** 環境変数 `PK_PRODUCTS`（JSON）で対応を決めます。未設定なら、商品IDに `unl`／`std`／`light` などの語を含むかで判定します。Google の `商品ID:基本プランID` は商品IDの部分で引きます。
- **テスト購入（SANDBOX）も反映します。** Apple の審査はテスト購入で行われるためです。止めたい時だけ `PK_IGNORE_SANDBOX=1` を設定してください。
- **500 を返すと、RevenueCat が後で再送します。**

### 9.3 アプリ（`index.html`）
- **`PKBilling`：** 次の2つがそろった時だけ購入できます。
  - `window.Capacitor.Plugins.Purchases`（RevenueCat の Capacitor プラグイン）がある
  - `window.PK_RC_KEYS={ios:'appl_…', android:'goog_…'}` が設定されている（RevenueCat の公開SDKキー）
  - 使う関数：`isConfigured`→`configure`／`logIn`（appUserID＝uid）、`getOfferings`（current の package を商品IDでプランに対応）、`purchasePackage`、`restorePurchases`
- **料金画面（`renderPlanExtras`、`openSheet('shPlan')` の時に描画）：**
  - 各プランに「このプランにする」ボタンを出します。利用中のプランは「ご利用中」です。
  - ストアの価格（`priceString`）で表示を置き換えます。
  - 「購入を復元」「購読の管理・解約」（iOS：apps.apple.com/account/subscriptions、Android：play.google.com/store/account/subscriptions）を出します。
  - 自動更新の説明と、利用規約・プライバシーポリシーへのリンクを出します。
  - 次回の更新日（または、自動更新を止めた時の利用期限）を出します。
  - Web で開いた時は、ボタンを押せません。「アプリから購入できます」と表示します。
- **購入・復元のあと（`billingConfirm`）：** `/api/auth` の `status` を2秒ごと・最大10回問い合わせ、サーバーへの反映を待ちます。間に合わない時は「数分後に開き直してください」と表示します。
- **マイページ：** 支払いの説明をストア課金向けにしました。次回の更新日も表示します。
- **文言：** `planNote` と `mpPay` を、10言語すべて「App Store / Google Play のアカウントで支払い」に変更しました。
- **チャットの送信：** `x-pk-auth`（ログインのトークン）を付けます。

### 9.4 `legal.html`（下書き。運営の確認が必要です）
- **利用規約（日英）：** ストアのアプリ内課金で支払うこと、24時間前までの解約、解約の方法（ストア）、退会しても購読は自動では解約されないこと、返金は各ストアの規定に従うことを書きました。
- **特定商取引法の表記：** 支払方法と解約・返金を、ストア課金向けに変更しました。販売価格はそのままです（ストアでの価格と合わせてください）。
- **プライバシーポリシー（日英）：** 外部送信先に Apple・Google・RevenueCat（アカウントを識別する番号と購入の記録のみ）を追加しました。
- **最終改定日：** 2026-09-26 にしました。

### 9.5 ストアの準備ができたら行う作業
1. **RevenueCat** でプロジェクトを作り、App Store と Google Play のアプリを登録します。
2. **ストアに月額の購読商品を3つ登録します**（例：`pk_light_monthly`／`pk_std_monthly`／`pk_unl_monthly`）。
   - RevenueCat に商品を取り込みます。
   - Entitlement を1つ（例：`pro`）作り、3商品を付けます。
   - Offering（current）に3つの Package を置きます。
3. **RevenueCat の Webhook を設定します。**
   - URL：`https://<サイト>/api/billing`
   - Authorization header：長いランダム文字列
4. **Netlify の環境変数を設定します。**
   - `REVENUECAT_WEBHOOK_AUTH`（3. と同じ文字列）
   - `PK_PRODUCTS`（例：`{"pk_light_monthly":"light","pk_std_monthly":"std","pk_unl_monthly":"unl"}`）
5. **ネイティブアプリ（Capacitor）を作ります。**
   - `@revenuecat/purchases-capacitor` を入れます。
   - 起動時に `window.PK_RC_KEYS` を設定します。
   - 商品IDの語でプランが判定できない場合は、`window.PK_PRODUCTS` も設定します。
6. **サンドボックス（テスト購入）で次を確認します。**
   - 購入 → 回数の上限が上がる
   - 解約 → 期限まで使える → 期限後は無料に戻る
   - 別の端末で「購入を復元」→ プランが戻る
7. **`LIMITS`（gemini.js）と、アプリの `PLAN_BUDGET`・`UNL_DAILY` を、最終のプラン内容に合わせます。**

### 9.6 検証（`pocketkantei_check/tests/`）
| 検証 | 結果 |
|---|---|
| `billtest.js`：通知の受け付け（合言葉なし・違い → 401、TEST、匿名ID → 無視、購入、古い通知の無視、プラン変更、解約 → 期限まで有効、期限切れ → free、Google形式の商品ID、不明な商品、TRANSFER、ログインの応答のプラン） | 全件期待どおり |
| `billtest2.js`：チャットの上限の判定（未購入 → free、購入後 → unl、トークン改ざん・なし → free、パスワード変更後の古いトークン → free） | 全件期待どおり |
| `billui.js`：ブラウザでの確認。RevenueCat プラグインを模擬しています。<br>・Web：ボタンを押せない＋案内（ja/en/th）<br>・未ログイン：アカウントの作成を案内<br>・価格の置き換え<br>・キャンセル：何も表示しない<br>・購入 → プランが有効・「ご利用中」・次回更新日・マイページの表示<br>・チャットにトークンが付く<br>・別端末で復元 | 全件期待どおり。画面エラー 0 |
| `ui10.js`・`e2e.js`・`e2e_reset.js`・`lpflow.js`・`eye.js`・`unitguard.js` | 画面エラー 0 |

### 9.7 ウェブ版のクレジットカード決済（テレコムクレジット）を追加（支払い方法は3つ）
運営の方針で、支払い方法は次の3つになりました。

| 使う場所 | 支払い方法 | 解約 |
|---|---|---|
| ウェブ版（ブラウザ・PWA） | クレジットカード（決済代行：テレコムクレジット） | 料金画面の「解約する」から利用者が自分で（運営の手作業なし） |
| iPhone アプリ | App Store のアプリ内課金 | App Store のアカウント設定 |
| Android アプリ | Google Play のアプリ内課金 | Google Play のアカウント設定 |

- **表示の切り替え：**
  - 料金画面は、`PKBilling.platform()` が `web` の時だけカード決済の案内（`BILL.webNote`・`webAuto`）を出します。
  - アプリ内では、カード決済を表示しません（Apple の審査ガイドラインにより、アプリ内で外部の決済へ誘導しないため）。
  - どこで買ったプランでも、同じアカウントでログインすれば、ウェブ版でもアプリでも使えます（`rec.sub.store` で購入元を区別）。
- **ウェブ版の購入の流れ：**
  - 「このプランにする」→ `/api/auth` の `checkout` → サーバーが決済画面のURLを作る → テレコムクレジットの決済画面へ移動します。
  - URLのひな形は、環境変数 `TELECOM_CHECKOUT_URLS`（JSON。プラン → URL）に入れます。`{uid}` `{email}` `{lang}` は置き換えます。
  - 未設定の間は「準備中」と表示します。
- **解約（手作業なし）：**
  - ウェブ版でカード決済の購読中の人には、料金画面に「解約する」ボタンを出します。
  - 押すと `/api/auth` の `cancel` が呼ばれます。サーバーは、決済会社の継続課金を止め（`telecomStopRecurring`）、成功したら `willRenew:false` にします。期限までは使えます。
  - `telecomStopRecurring` は、接続仕様書を受け取ってから実装します。今は `NOT_READY` を返すだけです。
  - アプリで買った人には「購読の管理・解約」（ストアの設定画面）を出します（`_manageHref`）。
  - 運営の問い合わせ窓口に解約を頼む流れは、なくしました。
- **価格：**
  - ウェブ版は、今の言語ごとの価格表示のままです（ja：980／1,980／2,980円、en：$9.99／$19.80／$29.80、zh：¥68／¥138／¥198 など）。
  - アプリは、ストアで登録した価格（`priceString`）を自動で表示します。
  - 特定商取引法の表記は「ウェブ版：円の価格／アプリ版：各ストアの表示価格」にしました。
- **規約類：** 利用規約、特定商取引法の表記、プライバシーポリシーを、3つの支払い方法に合わせて書き直しました（下書き・要確認）。
- **未実装（テレコムクレジットの接続仕様書が必要）：**
  1. **決済結果の通知の受け取り：** 決済が成功したら `rec.sub` に `{plan, expires, store:'TELECOM', willRenew:true}` を書き込みます。毎月の継続課金の成功・失敗も反映します。
  2. **決済画面のURLの作り方：** `clientip`・金額・利用者を見分ける値など、正確なパラメータ名が必要です。
  3. **解約（`telecomStopRecurring`）：** テレコムクレジットの継続課金を、こちらのサーバーから止める方法（API または解約用URL）を実装します。**運営の手作業は不可** という方針です。
  4. **ウェブ版を円以外の通貨で請求できるか：** 英語のドル表示など、海外向けの価格をテレコムクレジットで請求できるかを確認します。
- **検証：** `webcard.js` と `webcancel.js`（解約の接続がない時は「解約の手続きができませんでした」、ある時は解約されて期限まで利用可）で確認しました。
  - ウェブ版の表示（日本語・英語）
  - 決済URLが未設定の時は「準備中」になる
  - 設定すると、uid・メールアドレス・言語を入れた決済URLへ移動する
  - アプリ版（`billui.js`）は従来どおり動く
