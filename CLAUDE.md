# CLAUDE.md — このリポジトリの絶対ルール（毎回必ず守る）

四柱推命ツール（①個人版 `meishiki-original/` ②経験者版 `pro/`）。
作業前にこのファイルと `pro/ritsuun.html`（立運の教科書）を必ず読むこと。

## 四柱推命の計算・表示ルール（既定・絶対遵守 ／ 勝手に「標準ルール」に変えない）

大運・立運は必ず **この流派ルール** で計算・表示する。正典は `pro/ritsuun.html`。
一般的な標準ルールに置き換えたり、既定を変更したりしない。いつでもこのルール。

- **第一運＝月柱**：0歳〜立運までは月柱の干支がそのまま最初の運。
  **大運は必ず0歳＝月柱スタート**。
- **立運のスタート干支＝月柱の干支**（ここを変えない）。
- **順逆**：陽年生まれ男性／陰年生まれ女性＝順行。陰年男性／陽年女性＝逆行。
- **立運の出し方**：生まれてから節入りまで（順行＝次の節、逆行＝前の節）の日数を
  「3日＝1年／1日＝4ヶ月／6時間＝1ヶ月」で年齢に換算。
- **立運の端数（○ヶ月）は大運の年齢境界にも反映**する。
  例：立運8歳6ヶ月 → 「0才〜8才6ヶ月＝月柱」「8才6ヶ月〜18才6ヶ月」…
- **大運の干支**は月柱を起点に、順逆の向きへ1つずつ進める。
- 検算の基準例：**1981-06-10 22:28 女性** →
  月柱 **甲午** ／ **順行** ／ 立運 **8歳6ヶ月** ／ 大運 **甲午→乙未→丙申→丁酉**。
  （この結果に一致しない実装は誤り）

## 触ってはいけないもの

- 計算エンジン（`pklove.js` / `window.Bazi` / `computeChart` / `computeMeishiki`）は**改変しない**。
  変更は表示・アダプタ層のみ。鑑定結果の見せ方だけを扱う。
- `pro/meishi-sheet.html` は `meishiki-original/meishi-sheet.html` に**バイト単位でミラー**する。
- 変更のたびに `pro/sw.js` の `CACHE` バージョンを1つ上げる。

## 表現ルール

- 鑑定文にアスタリスク（`*`）を出さない。
- サイト内で「AI」という語を使わない（「詳細鑑定」等の言い換え）。

## 会局の吉凶（扶抑）

- 会局の吉凶は身強弱（扶抑）で判定。**自動判定を初期表示＋鑑定者が上書き可**。中和は色なし。
- 命式データ（四柱・大運・年運・空亡・会局）は鑑定文と命式表で同じエンジン結果から連動させる。
