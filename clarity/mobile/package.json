{
  "name": "numinous-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell for the Numinous app (App Store / Google Play)",
  "scripts": {
    "www": "bash build-www.sh",
    "sync": "npm run www && npx cap sync",
    "ios": "npm run sync && npx cap open ios",
    "android": "npm run sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@revenuecat/purchases-capacitor": "^9.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
